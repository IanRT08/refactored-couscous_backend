# ATENCIÓN: este perfil (dev) NUNCA debe activarse en producción.
# Para desarrollo local, preferir el perfil "local" (application-local.properties).
# En prod, SPRING_PROFILES_ACTIVE no debe incluir "dev".
server.port=8085
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
